spring:
  profiles:
    include: secret
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        hbm2ddl:
          auto: create
logging.level:
  org.hibernate.SQL: debug


